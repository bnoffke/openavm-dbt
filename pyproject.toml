[project]
name = "my-dbt-project"
version = "0.1.0"
description = "dbt project with DuckDB, spatial extension, and SFTP support"
requires-python = ">=3.10"
dependencies = [
    "dbt-core>=1.10.13",
    "dbt-duckdb>=1.9.6",
]
